// const addVueRawPreLoader = require("../vue-raw-pre");

module.exports = {
	chainWebpack: (config) => {
		// addVueRawPreLoader(config, {
		// 	componentName: "example-demo",
		// 	propName: "code"
		// });
	},
};